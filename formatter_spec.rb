RSpec.describe "A spec file to demonstrate RSpec formatter work" do
 context "when running some text" do
    it "the test usually calls the expect() at least once" do
      expect(1 + 1).to eq(1)
    end 
 end	
end